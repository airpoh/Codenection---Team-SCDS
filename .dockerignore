# Do NOT ignore backend or backend/aa-test. We need them in the Docker build context.

# VCS
.git
.gitignore

# Python caches
**/__pycache__/
**/*.pyc
**/*.pyo

# OS junk
.DS_Store

# Node/Yarn logs (keep node_modules for aa-test!)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (safe to ignore if present at repo root)
dist/
build/

# Editor configs
.vscode/
.idea/

# Do NOT add node_modules here because backend/aa-test/node_modules is needed in final image
