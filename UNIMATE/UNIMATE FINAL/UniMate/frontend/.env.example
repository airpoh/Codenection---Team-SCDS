# ============================================================================
# UniMate Frontend Environment Configuration (React Native + Expo)
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================

# Backend API Base URL
# For local development:
#   - Use your computer's local IP (not localhost/127.0.0.1)
#   - Find your IP: ifconfig | grep "inet " | grep -v 127.0.0.1 (Mac/Linux)
#   - Find your IP: ipconfig (Windows)
#   - Example: http://10.72.127.211:8000
# For production:
#   - Use your deployed backend URL
#   - Example: https://api.unimate.app
API_BASE_URL=http://10.72.127.211:8000

# ============================================================================
# EXPO CONFIGURATION (Optional)
# ============================================================================

# Expo project ID (if using EAS)
# EXPO_PROJECT_ID=your-expo-project-id

# ============================================================================
# NOTES FOR DEVELOPERS
# ============================================================================
#
# 1. FINDING YOUR LOCAL IP:
#    - macOS/Linux: Run `ifconfig | grep "inet " | grep -v 127.0.0.1`
#    - Windows: Run `ipconfig` and look for IPv4 Address
#    - Use the IP that starts with 192.168.x.x or 10.x.x.x
#
# 2. TESTING ON PHYSICAL DEVICE:
#    - Your phone and computer must be on the same WiFi network
#    - Use your computer's local IP address (not localhost)
#    - Example: http://192.168.1.39:8000
#
# 3. TESTING ON EMULATOR/SIMULATOR:
#    - iOS Simulator: Use http://localhost:8000 or your local IP
#    - Android Emulator: Use http://10.0.2.2:8000 (special Android alias)
#    - Or use your local IP for both
#
# 4. PRODUCTION DEPLOYMENT:
#    - Update API_BASE_URL to your production backend URL
#    - Ensure HTTPS is used in production
#    - Example: https://api.unimate.app
#
